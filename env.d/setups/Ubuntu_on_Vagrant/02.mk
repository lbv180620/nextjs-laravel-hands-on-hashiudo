#todo 2.  開発ツールの導入

# 開発に必要なモジュール

# sudo apt install -y curl git ruby

# sudo apt install -y openjdk-16-jdk

# sudo apt install -y zlib1g-dev libssl-dev libbz2-dev libsqlite3-dev libffi-dev liblzma-dev

# sudo apt install -y software-properties-common

# sudo apt install -y apt-transport-https ca-certificates

#  sudo apt install -y build-essential

# { 一回でやるなら
# sudo apt install -y curl git ruby openjdk-16-jdk \
# zlib1g-dev libssl-dev libbz2-dev libsqlite3-dev libffi-dev liblzma-dev \
# software-properties-common apt-transport-https ca-certificates build-essential
# }

# //sudo apt install -y Apache2-dev

# sudo mkdir /downloads /ext /sample

# --------------------

# # linuxbrew

# sh -c "$(curl -fsSL https://raw.githubusercontent.com/Linuxbrew/install/master/install.sh)"

# echo 'export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"' >> ~/.zshrc

# ＊パスは最初の方に書かないと通らない

# source ~/.zshrc

# brew install curl git wget gcc zlib libzip bzip2 readline openssl pkg-config autoconf

# --------------------

# sudo apt install -y tree net-tools expect iproute2
# sudo apt-get install -y install traceroute
# sudo apt-get install -y ack

# sudo apt install traceroute
